Lỗi api http://localhost:8888/api/courses/14/export-results?format=excel khi xuất excel kết quả điểm ra data rỗng, chỉ có tiêu đề mà không có điểm số.

Lỗi api http://localhost:8888/api/questions/import-excel import câu hỏi từ excel để import thành công mà xem lại thì không thấy hiển thị?

Lỗi xóa môn học rồi mà api PLO vẫn trả về môn học đó trong api http://localhost:8888/api/plos/program/1
